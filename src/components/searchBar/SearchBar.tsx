import React from 'react'
import { styled } from '@mui/material'
import { motion } from 'framer-motion'

const SearchBarContainer = styled(motion.div)(({ theme }) => ({
    display: 'flex',
    flexDirection: 'column',
    width: '34em'
}))

const SearchBar = () => {

}